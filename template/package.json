{
  "name": "<%= name %>",
  "version": "0.0.0",
  "description": "<%= description %>",
  "keywords": [
    "<%= generator %>",
    "cooking",
    "slushgenerator"
  ],
  "scripts": {
    "dev": "cooking watch",
    "dist": "cooking build"
  },<% if (github) { %>
  "repository": {
    "type": "git",
    "url": "git+<%= github %>.git"
  },
  "homepage": "<%= github %>#readme",
  "bugs": {
    "url": "<%= github %>/issues"
  },<% } %><% if (author) { %>
  "author": "<%= author %>",<% } %>
  "license": "<%= license %>"
}